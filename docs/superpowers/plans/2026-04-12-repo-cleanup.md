# Repo Cleanup & Organisation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Delete stale files, correct outdated metadata, and organise deployment configs so the repo accurately reflects the current production state of TLDR Shield v2.0.0.

**Architecture:** No code changes — this plan is pure housekeeping: deletes, small JSON/metadata edits, and moving two config files into a dedicated folder.

**Tech Stack:** Git, Node.js (npm), JSON files, Markdown.

---

## File Map

| Action | File | Reason |
|--------|------|--------|
| DELETE | `index.html` (root) | Stale Vite scaffold stub — `dist/index.html` is generated by `npm run build` |
| DELETE | `list_models.ts` (root) | One-off NIM model lister — not imported anywhere, not in CI |
| DELETE | `models.json` (root) | Auto-generated output of `list_models.ts` — stale and not referenced |
| DELETE | `AGENTS.md` (root) | Duplicate of `CLAUDE.md`; references "Codex" (wrong tool); `CLAUDE.md` is the canonical guide |
| DELETE | `.claude/session-notes.md` | Completely stale — says "backend NOT deployed to Cloud Run" when it has been live since before Phase 2 |
| EDIT | `package.json` | `name` is `"react-example"` (scaffold default); remove `@google/genai` (listed as dep, never imported) |
| EDIT | `metadata.json` | Description says "X2.0" and "Multi-Model Judge" — neither exists; update to reflect Phase 2 actual features |
| EDIT | `firebase-blueprint.json` | Missing `shared_cache` and `reports` collections; `Scan` entity missing `url`, `tier`, `model`, `cached`, `latencyMs` fields; rating description says "DANGER" (old name, now "RISKY") |
| EDIT | `extension/manifest.json` | Version is `"2.0"` — Chrome expects a `major.minor.patch` string; update to `"2.0.0"` |
| EDIT | `CLAUDE.md` | "Current State" table still lists Phase 2 UX polish as not done; update to complete |
| MOVE | `railway.json` → `docs/deployment/railway.json` | Deployment config belongs in docs/deployment, not repo root |
| MOVE | `nixpacks.toml` → `docs/deployment/nixpacks.toml` | Same reason as above |
| REMOVE | `.worktrees/phase-2` worktree | PR #4 (phase-2 → master) was merged; worktree is an orphan |

---

### Task 1: Delete stale root files

**Files:**
- Delete: `index.html`
- Delete: `list_models.ts`
- Delete: `models.json`
- Delete: `AGENTS.md`

- [ ] **Step 1: Confirm each file is safe to delete**

  Verify none of the files are referenced:
  ```bash
  grep -r "AGENTS.md\|list_models\|models.json" --include="*.ts" --include="*.js" --include="*.json" --include="*.yml" .
  ```
  Expected: no matches (or only `package-lock.json` for the dep — which is fine).

  Verify `index.html` at root is NOT the same as `dist/index.html` (it shouldn't be — `dist/` is gitignored):
  ```bash
  head -3 index.html
  ```
  Expected: something like `<!DOCTYPE html>` with a Vite/scaffold comment. Not a built artifact.

- [ ] **Step 2: Delete the four files**

  ```bash
  rm index.html list_models.ts models.json AGENTS.md
  ```

- [ ] **Step 3: Verify git status**

  ```bash
  git status
  ```
  Expected: four files shown under "Changes not staged" or "Changes to be committed" as deleted.

- [ ] **Step 4: Commit**

  ```bash
  git add -u index.html list_models.ts models.json AGENTS.md
  git commit -m "chore: delete stale root files (scaffold stub, one-off scripts, duplicate guide)"
  ```

---

### Task 2: Delete stale .claude/session-notes.md

**Files:**
- Delete: `.claude/session-notes.md`

- [ ] **Step 1: Confirm the file is stale**

  Read the file:
  ```bash
  cat .claude/session-notes.md
  ```
  Expected: contains "Backend NOT deployed to Cloud Run yet" — the backend has been live on Cloud Run since before Phase 2 started. Confirmed stale.

- [ ] **Step 2: Delete it**

  ```bash
  rm .claude/session-notes.md
  ```

- [ ] **Step 3: Commit**

  `.claude/` is gitignored — but confirm first:
  ```bash
  git check-ignore -v .claude/session-notes.md
  ```
  - If gitignored: no commit needed. Done.
  - If tracked: `git rm .claude/session-notes.md && git commit -m "chore: delete stale session notes"`

---

### Task 3: Fix package.json — name and unused dependency

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Rename the package**

  In `package.json` line 2, change:
  ```json
  "name": "react-example",
  ```
  to:
  ```json
  "name": "tldr-shield",
  ```

- [ ] **Step 2: Remove @google/genai**

  Confirm it is genuinely unused (not imported in any .ts/.js file):
  ```bash
  grep -r "@google/genai" --include="*.ts" --include="*.js" . --exclude-dir=node_modules
  ```
  Expected: no matches.

  Then remove it:
  ```bash
  npm uninstall @google/genai
  ```

- [ ] **Step 3: Verify lint still passes**

  ```bash
  npm run lint
  ```
  Expected: exits 0 with no errors.

- [ ] **Step 4: Commit**

  ```bash
  git add package.json package-lock.json
  git commit -m "chore: rename package to tldr-shield, remove unused @google/genai dep"
  ```

---

### Task 4: Update metadata.json — accurate description

**Files:**
- Modify: `metadata.json`

Current (inaccurate):
```json
{
  "name": "TLDR Shield",
  "description": "Advanced X2.0 AI-powered Privacy Shield with Multi-Model Judge, ELI5 mode, and Dark Pattern detection.",
  "requestFramePermissions": []
}
```

Problems:
- "X2.0" — not a feature name, marketing fluff from an early draft
- "Multi-Model Judge" — aspirational feature, never built; using it in the description is misleading

- [ ] **Step 1: Update the description**

  Replace the entire `metadata.json` content with:
  ```json
  {
    "name": "TLDR Shield",
    "description": "AI-powered privacy scanner that rates Terms of Service pages as SAFE, OKAY, or RISKY — with verbatim citations, ELI5 explanations, and dark pattern detection.",
    "requestFramePermissions": []
  }
  ```

- [ ] **Step 2: Commit**

  ```bash
  git add metadata.json
  git commit -m "chore: update metadata.json description to reflect actual Phase 2 features"
  ```

---

### Task 5: Update firebase-blueprint.json — complete schema

**Files:**
- Modify: `firebase-blueprint.json`

Current problems:
- `Scan.rating` description says `"DANGER"` — the actual value is `"RISKY"`
- `Scan` entity missing fields that `server.ts` actually writes: `url`, `tier`, `model`, `cached`, `latencyMs`
- Missing two entire collections: `/shared_cache/{hash}` and `/reports/{id}`

- [ ] **Step 1: Replace firebase-blueprint.json with the corrected version**

  ```json
  {
    "entities": {
      "Scan": {
        "title": "Scan",
        "description": "A privacy policy scan result stored per-user.",
        "type": "object",
        "properties": {
          "uid":       { "type": "string",  "description": "Firebase UID of the user who ran the scan." },
          "url":       { "type": "string",  "description": "URL of the scanned page." },
          "rating":    { "type": "string",  "description": "Overall rating: SAFE | OKAY | RISKY" },
          "score":     { "type": "number",  "description": "Numerical score 0–100." },
          "tldr":      { "type": "string",  "description": "Plain-English one-paragraph summary." },
          "pillars":   { "type": "object",  "description": "Per-pillar breakdown. null for Quick scans; object for Deep scans." },
          "tier":      { "type": "string",  "description": "Scan tier: quick | deep" },
          "model":     { "type": "string",  "description": "NIM model ID used for the scan (e.g. meta/llama-3.3-70b-instruct)." },
          "cached":    { "type": "boolean", "description": "true if the result was served from L2 Firestore cache." },
          "latencyMs": { "type": "number",  "description": "Total analysis latency in milliseconds." },
          "createdAt": { "type": "string",  "format": "date-time", "description": "ISO timestamp when the scan was created." }
        },
        "required": ["uid", "url", "rating", "score", "tldr", "tier", "createdAt"]
      },
      "User": {
        "title": "User",
        "description": "User profile with monthly credit tracking.",
        "type": "object",
        "properties": {
          "uid":            { "type": "string", "description": "Firebase UID." },
          "credits":        { "type": "number", "description": "Remaining scan credits (resets to 400 on the 1st of each month)." },
          "lastResetMonth": { "type": "string", "description": "Last reset month in YYYY-MM format." },
          "role":           { "type": "string", "description": "User role. Currently only 'admin' is used." }
        },
        "required": ["uid", "credits", "lastResetMonth"]
      },
      "SharedCache": {
        "title": "SharedCache",
        "description": "Anonymous cross-user result cache. Written by Admin SDK only — client access is blocked by Firestore rules.",
        "type": "object",
        "properties": {
          "result":        { "type": "object", "description": "Full scan result payload (same shape as Scan minus uid)." },
          "tier":          { "type": "string", "description": "quick | deep" },
          "scannedAt":     { "type": "string", "format": "date-time", "description": "When the original scan was run." },
          "expiresAt":     { "type": "string", "format": "date-time", "description": "Cache expiry: 48h for Quick, 7d for Deep." },
          "scanCount":     { "type": "number", "description": "Number of times this cached result has been served." },
          "sourceUrlHash": { "type": "string", "description": "SHA-256 of the source URL (for lookup without exposing full URL)." }
        },
        "required": ["result", "tier", "scannedAt", "expiresAt"]
      },
      "Report": {
        "title": "Report",
        "description": "User-submitted feedback on an incorrect scan result.",
        "type": "object",
        "properties": {
          "uid":       { "type": "string", "description": "Firebase UID of the reporter." },
          "url":       { "type": "string", "description": "URL of the scanned page being reported." },
          "rating":    { "type": "string", "description": "The rating the user believes is wrong." },
          "score":     { "type": "number", "description": "The score the user believes is wrong." },
          "pillars":   { "type": "object", "description": "Pillar breakdown from the disputed scan." },
          "requestId": { "type": "string", "description": "X-Request-ID from the original analysis response." },
          "createdAt": { "type": "string", "format": "date-time", "description": "When the report was submitted." }
        },
        "required": ["uid", "url", "rating", "createdAt"]
      }
    },
    "firestore": {
      "/scans/{scanId}": {
        "schema": "Scan",
        "description": "Per-user scan history. Document ID is auto-generated."
      },
      "/users/{userId}": {
        "schema": "User",
        "description": "User profile and credit balance. Document ID is the Firebase UID."
      },
      "/shared_cache/{hash}": {
        "schema": "SharedCache",
        "description": "Anonymous cross-user result cache. Document ID is SHA-256(text + tier + eli5 + darkPatterns + modelId). Client-read-blocked."
      },
      "/reports/{reportId}": {
        "schema": "Report",
        "description": "User feedback on incorrect results. Document ID is auto-generated."
      }
    }
  }
  ```

- [ ] **Step 2: Commit**

  ```bash
  git add firebase-blueprint.json
  git commit -m "docs: update firebase-blueprint.json — add shared_cache + reports, fix Scan fields"
  ```

---

### Task 6: Fix extension/manifest.json version

**Files:**
- Modify: `extension/manifest.json`

Chrome extension versions must be in `major.minor.patch` format. The current value `"2.0"` is technically valid but inconsistent with `package.json` (`2.0.0`) and the README badge (`2.0.0`).

- [ ] **Step 1: Update version field**

  In `extension/manifest.json`, find:
  ```json
  "version": "2.0",
  ```
  Replace with:
  ```json
  "version": "2.0.0",
  ```

- [ ] **Step 2: Commit**

  ```bash
  git add extension/manifest.json
  git commit -m "chore: align extension manifest version to 2.0.0 (matches package.json)"
  ```

---

### Task 7: Update CLAUDE.md — mark Phase 2 complete

**Files:**
- Modify: `CLAUDE.md`

The "Current State" table still lists Phase 2 UX polish as a gap. It was completed in the `phase-2` branch (merged as PR #4 today).

- [ ] **Step 1: Update the Known Gaps section**

  Find the following lines in `CLAUDE.md`:
  ```
  - No NIM key health monitoring (key failures are reactive, not proactive)
  - No user-facing policy change notifications (manual re-scan only)
  - Extension popup, badge UI, and dashboard are prototype-grade (Phase 2 UX polish not yet done)
  ```

  Replace with:
  ```
  - No NIM key health monitoring (key failures are reactive, not proactive)
  - No user-facing policy change notifications (manual re-scan only)
  - ~~Extension popup, badge UI, and dashboard are prototype-grade~~ ✅ Phase 2 UX polish complete (popup redesign, skeleton screen, history dashboard, landing page, Chrome side panel)
  ```

- [ ] **Step 2: Update the Repository Layout section**

  The layout lists `extension/` files. Add `sidepanel.html/.js` which were added in Phase 2:

  Find:
  ```
  │   ├── popup.html/.js      # Tier picker, ELI5/dark-patterns flags, sign-in, credits display
  │   ├── offscreen.html/.js  # PDF text extraction via pdf.js (MV3 requires offscreen doc)
  ```

  Replace with:
  ```
  │   ├── popup.html/.js      # Tier picker, ELI5/dark-patterns flags, sign-in, credits display
  │   ├── sidepanel.html/.js  # Chrome Side Panel — shows full scan result with live progress
  │   ├── offscreen.html/.js  # PDF text extraction via pdf.js (MV3 requires offscreen doc)
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add CLAUDE.md
  git commit -m "docs: mark Phase 2 UX polish complete in CLAUDE.md, add sidepanel to layout"
  ```

---

### Task 8: Move railway.json and nixpacks.toml to docs/deployment/

**Files:**
- Move: `railway.json` → `docs/deployment/railway.json`
- Move: `nixpacks.toml` → `docs/deployment/nixpacks.toml`
- Create: `docs/deployment/README.md` (brief note on what these files are for)

These are Railway/Nixpacks deployment configs. The live production deployment uses Google Cloud Run, but these are kept for reference. They should not clutter the repo root.

- [ ] **Step 1: Create the deployment docs folder and move files**

  ```bash
  mkdir -p docs/deployment
  git mv railway.json docs/deployment/railway.json
  git mv nixpacks.toml docs/deployment/nixpacks.toml
  ```

- [ ] **Step 2: Add a README explaining what they are**

  Create `docs/deployment/README.md` with this content:
  ```markdown
  # Deployment Configs

  ## Active deployment: Google Cloud Run

  ```bash
  npm run deploy
  # Runs: gcloud run deploy tldr-shield --source . --region us-central1 --project tldr-493003 --allow-unauthenticated
  ```

  The `predeploy` script runs lint → build → golden tests → quick eval before deploying.
  Cloud Run uses Application Default Credentials — no service account file needed.

  ---

  ## Alternative configs (not currently active)

  | File | Platform | Notes |
  |------|----------|-------|
  | `railway.json` | Railway | Node.js service config |
  | `nixpacks.toml` | Railway / Nixpacks | Build config (Node 20, `npm run build && npm start`) |
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add docs/deployment/
  git commit -m "chore: move railway.json + nixpacks.toml to docs/deployment/"
  ```

---

### Task 9: Remove orphaned phase-2 worktree

**Files:**
- Remove: `.worktrees/phase-2/` (git worktree, not a regular directory)

PR #4 (`phase-2` → `master`) was merged. The worktree at `.worktrees/phase-2` is an orphan.

- [ ] **Step 1: Confirm the worktree exists and its branch is merged**

  ```bash
  git worktree list
  git branch -r | grep phase-2
  ```
  Expected: worktree listed at `.worktrees/phase-2`; remote branch `origin/phase-2` exists (not deleted yet).

- [ ] **Step 2: Remove the worktree**

  ```bash
  git worktree remove .worktrees/phase-2
  ```
  Expected: `.worktrees/phase-2` directory removed.

- [ ] **Step 3: Optionally delete the remote branch** (only if confident PR is fully merged)

  ```bash
  git push origin --delete phase-2
  ```

- [ ] **Step 4: Verify**

  ```bash
  git worktree list
  ```
  Expected: only the main worktree listed.

  No commit needed — worktree removal does not create a git commit.

---

## Priority Summary

| Priority | Task | Time estimate |
|----------|------|---------------|
| High | Task 1 — Delete stale root files | 2 min |
| High | Task 3 — Fix package.json | 3 min |
| High | Task 9 — Remove orphaned worktree | 2 min |
| Medium | Task 2 — Delete session-notes.md | 1 min |
| Medium | Task 4 — Update metadata.json | 2 min |
| Medium | Task 5 — Update firebase-blueprint.json | 5 min |
| Medium | Task 6 — Fix manifest version | 1 min |
| Medium | Task 7 — Update CLAUDE.md | 3 min |
| Low | Task 8 — Move railway.json + nixpacks.toml | 3 min |

---

*Plan created: 2026-04-12*
